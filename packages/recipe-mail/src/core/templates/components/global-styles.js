const styles = {
    grid: {
        columns: 2,
        gutter: 20,
        justifyContent: 'left',
        responsive: true,
        maxWidth: 600,
        verticalAlign: 'top',
    },
};

export default styles;
